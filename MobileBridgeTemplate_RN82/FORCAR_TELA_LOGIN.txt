PASSOS PARA GARANTIR QUE A TELA DE LOGIN APAREÇA
==================================================

O código está CORRETO e COMPLETO. Se a tela de login não está aparecendo,
é porque você provavelmente JÁ ESTÁ LOGADO (token JWT salvo no dispositivo).

SOLUÇÃO 1 - Deletar o app e reinstalar (MAIS RÁPIDO)
----------------------------------------------------

No PowerShell:
```powershell
# 1. Deletar o app do dispositivo
adb uninstall com.mobilebridgeapp

# 2. Reinstalar
npx react-native run-android
```

Isso vai apagar todos os dados salvos e forçar você a fazer login novamente.


SOLUÇÃO 2 - Limpar storage do app via ADB
------------------------------------------

No PowerShell:
```powershell
# Limpar dados do app sem desinstalar
adb shell pm clear com.mobilebridgeapp

# Reabrir o app
adb shell am start -n com.mobilebridgeapp/.MainActivity
```


SOLUÇÃO 3 - Limpar via código
------------------------------

Se o app já estiver aberto:

1. Abra o React Native DevTools (pressione 'd' no emulador ou shake no dispositivo real)
2. Abra o console JavaScript
3. Execute:

```javascript
// Importar AuthService
const AuthService = require('./src/services/AuthService').AuthService;

// Fazer logout
AuthService.logout()
  .then(() => {
    console.log('✅ Logout realizado! Recarregue o app.');
  })
  .catch(err => console.error('❌ Erro:', err));
```

4. Depois recarregue o app (pressione 'r' duas vezes no terminal do Metro)


SOLUÇÃO 4 - Rebuild completo
-----------------------------

No PowerShell:
```powershell
# 1. Parar Metro (Ctrl+C)

# 2. Limpar tudo
Remove-Item -Recurse -Force android\app\build
Remove-Item -Recurse -Force android\build
adb uninstall com.mobilebridgeapp

# 3. Rebuild
cd android
.\gradlew clean
cd ..

# 4. Iniciar Metro
npm start -- --reset-cache

# 5. Em OUTRO terminal, rodar app
npx react-native run-android
```


COMO SABER SE FUNCIONOU?
-------------------------

Ao abrir o app, você deve ver esta sequência:

1. Tela branca/cinza com:
   "Verificando autenticação..."
   (Loading spinner)

2. Depois de 1-2 segundos:
   TELA DE LOGIN com:
   - Logo 🛍️
   - "Mobile Bridge App"
   - "Faça login para continuar"
   - Campo Email
   - Campo Senha
   - Botão "Entrar"
   - Botão "💡 Usar credenciais de teste"
   - Card azul com usuário: usuario@teste.com

Se aparecer direto a WebView com produtos = você está logado!


LOGS PARA VERIFICAR
-------------------

No Metro Bundler, procure por estas mensagens:

SE NÃO LOGADO (tela de login aparece):
```
[Auth] Service initialized, user: undefined
```

SE JÁ LOGADO (vai direto para WebView):
```
[Auth] Service initialized, user: usuario@teste.com
[App] User authenticated: usuario@teste.com
[Auth] JWT Token: eyJhbGci...
```


TESTAR O LOGIN
--------------

1. Clique em "💡 Usar credenciais de teste"
2. Campos são preenchidos:
   - Email: usuario@teste.com
   - Senha: senha123
3. Clique em "Entrar"
4. Loading aparece
5. Toast verde: "Bem-vindo! Login realizado com sucesso"
6. App redireciona para WebView

O token JWT é salvo automaticamente e nas próximas vezes
o app vai direto para a WebView.


DICA
----

Durante desenvolvimento, use sempre:
```powershell
adb uninstall com.mobilebridgeapp && npx react-native run-android
```

Isso garante instalação limpa sem cache de autenticação.
